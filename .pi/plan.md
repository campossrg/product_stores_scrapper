# Plan: Add automatic selector detection and hide manual fields by default

## Step 1: Review the scraper flow and UI components that currently require manual selectors, including custom scrape and schedule creation paths.
- **Files**: lib/scraper.ts, lib/types.ts, app/api/scrape/route.ts, app/api/schedule/route.ts, components/ScraperControl.tsx, components/ScheduleManager.tsx

## Step 2: Implement selector auto-detection for supported pages (starting with Mas Can Cadell) and add a fallback heuristic so the backend can scrape from URL/store info without the user entering CSS selectors manually.
- **Files**: lib/scraper.ts, lib/types.ts, app/api/scrape/route.ts, app/api/schedule/route.ts

## Step 3: Update the UI so users only enter website URL and store name by default, moving manual selector inputs behind an optional advanced section or removing them from the main flow.
- **Files**: components/ScraperControl.tsx, components/ScheduleManager.tsx

## Step 4: Adjust or add tests for the new auto-detect behavior and defaults.
- **Files**: __tests__/lib/scraper.test.ts, __tests__/app/api/schedule.test.ts
- **Command**: `npm test -- --runInBand`

## Step 5: Run validation to confirm the app still builds and tests pass.
- **Command**: `npm test -- --runInBand`

---
*Plan generated by plan-execute workflow*
